import React from 'react'

const Home = () => {
    return (
        <div>
            <h1>Home</h1>
            <p>
                To retrieve the channel logo channel thumbnail using the YouTube Data API v3 with an Axios request in a React application, you can follow these steps:

                Install Axios: Start by installing the Axios library in your React project. Open your terminal and run the following command:
            </p>
        </div>
    )
}

export default Home